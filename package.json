{
  "dependencies": {
    "cypress": "^12.6.0"
  },
  "scripts": {
    "cypress": "cypress",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "mochawesome": "^7.1.3"
  }
}
